{
  "name": "clappone-cpa-genie",
  "version": "0.1.0",
  "private": true,
  "description": "ClAppOne CPA Genie portal for CPA firms, clients, tax document workflows, and AI-assisted return preparation.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "node scripts/serve.mjs",
    "start:api": "node apps/api/src/server.mjs",
    "check": "node --check apps/api/src/server.mjs && node --check apps/api/src/config.mjs && node --check apps/api/src/azureOpenAi.mjs && node --check scripts/build.mjs && node --check scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
